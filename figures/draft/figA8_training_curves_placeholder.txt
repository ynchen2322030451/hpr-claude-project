Figure A8 — Training Curves (NLL vs. Epoch)
============================================

通俗说明 / PLAIN LANGUAGE EXPLANATION
─────────────────────────────────────

This figure is a placeholder — it will show the actual training curves once
the training history files are generated by running the training script.

本图当前为占位图——待运行训练脚本生成训练历史文件（training_history_level*.csv）后，
将替换为真实的训练曲线。

When populated, this figure will show:
- How the training and validation loss (NLL) decreases over training epochs
- The epoch at which early stopping fired (best validation NLL)
- Comparison between Level 0 (baseline) and Level 2 (physics-regularized)

填充后，本图将展示：训练集和验证集负对数似然损失（NLL）随训练轮次的下降曲线；
触发早停时的最优验证NLL对应轮次；Level 0（基线）与Level 2（物理正则化）两种模型的
收敛过程对比。

Expected pattern: both models should show decreasing NLL, with Level 2
converging to a lower final validation NLL (0.305 vs. 0.359 for Level 0).
The physics regularization term L_mono may slow initial convergence but
improve the final validation NLL and OOD generalization.

预期规律：两个模型的NLL均应随训练逐步下降，Level 2最终达到更低的验证NLL（0.305，
对比Level 0的0.359）。物理正则化项L_mono可能使初始收敛略有减慢，但有助于改善最终
验证NLL和分布外泛化性能。

TO GENERATE: run run_train_fixed_surrogates.py (or run_train_0404.py with the
appropriate config) to produce the training history CSV files.

生成方法：运行 run_train_fixed_surrogates.py（或以适当配置运行 run_train_0404.py），
即可生成所需的训练历史CSV文件，替换此占位图。


严谨说明 / RIGOROUS EXPLANATION
─────────────────────────────────

Figure A8 (v1, when available): Training NLL curves for both surrogate levels.
Two panels (Level 0 left, Level 2 right), each showing:
- Training NLL vs. epoch (solid line)
- Validation NLL vs. epoch (dashed line)
- Vertical dotted line at the best validation NLL epoch (early stopping point)

图A8（v1，待生成）：两个代理层次的训练NLL曲线。左右两个面板分别对应Level 0和Level 2，
各面板展示：训练集NLL随轮次变化（实线）、验证集NLL随轮次变化（虚线）、最优验证NLL对应
轮次的早停标记（竖向点线）。

Figure A8 (v2, when available): Single panel showing validation NLL for both
models on the same axes, enabling direct comparison of convergence rates and
final performance.

图A8（v2，待生成）：单面板展示两个模型的验证集NLL曲线（同轴绘制），便于直接对比
两种模型的收敛速率和最终性能。

Expected values at convergence:
- Level 0: val NLL ≈ 0.359 (test NLL)
- Level 2: val NLL ≈ 0.305 (test NLL)

收敛时预期值：Level 0验证NLL≈0.359，Level 2验证NLL≈0.305（均与测试集NLL一致）。

Source files to be generated:
  fixed_surrogate_fixed_base/training_history_level0.csv
  fixed_surrogate_fixed_level2/training_history_level2.csv

待生成来源文件：
  fixed_surrogate_fixed_base/training_history_level0.csv
  fixed_surrogate_fixed_level2/training_history_level2.csv
